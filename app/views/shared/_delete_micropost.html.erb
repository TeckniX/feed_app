<% if current_user?(post_item.user) %>
  <%= link_to "delete", post_item, method: :delete,
                                   data: { confirm: "You sure?" },
                                   title: post_item.content %>
<% end %>